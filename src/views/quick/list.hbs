<div class="container p-4">
    <div class="row">
        {{#each quick}}
        <div class="col-md-3">
            <div class="card text-center">
                <div class="card-body">
                    {{!-- <a href="{{text}}" target="_blank"> --}}
                        <h3 class="card-title text-uppercase">
                            {{tex}}
                        </h3>
                    </a>
                    <p class="m-2">{{text}}</p>
                    <p class="text-muted">
                        {{timeago created_at}}
                    </p>
                    <a class="btn btn-danger" href="/quick/delete/{{id}}">
                        Delete Link
                    </a>
                    <a class="btn btn-secondary" href="/quick/edit/{{id}}">
                        Edit
                    </a>
                </div>
            </div>
        </div>
        {{else}}
        <div class="col-md-4 mx-auto">
            <div class="card card-body text-center">
                <p>There are not Links saved yet.</p>
                <a href="/quick/add">Create One!</a>
            </div>
        </div>
        {{/each}}
    </div>

      <div class="col-md-4 mx-auto">
            <div class="card card-body text-center">
                {{!-- <p>There are not Links saved yet.</p> --}}
                <a href="/quick/add">Create nueva nota rapida!</a>
            </div>
        </div>
</div>